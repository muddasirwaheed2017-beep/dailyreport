# Runtime state — belongs on the deploy host, not in git
store/
auth/
logs/
checkpoint.json
context.json
groups.json
*.corrupt-*
.env

# probe artefacts
whatsapp-qr.png
whatsapp-qr.html
.wwebjs_auth/
.wwebjs_cache/
